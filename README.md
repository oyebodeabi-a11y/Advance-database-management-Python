# Advance-database-management-Python
This project focuses on designing and implementing an advanced database management system (DBMS) using Python integrated with a MySQL database. The system is built to efficiently store, manage, and manipulate structured data across multiple related tables while ensuring data integrity, scalability, and performance.

To fix the error:
I am having import mysql.connector error. What libraries do I need to install on my machine.

Fix: 
pip3 install mysql-connector-python

